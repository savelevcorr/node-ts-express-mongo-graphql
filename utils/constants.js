const DEST_FOLDER = 'dist';
const SRC_FOLDER = './src'
const TS_SRC_FILES = `${SRC_FOLDER}/**/*.ts`;
const CSS_SRC_FILES = `${SRC_FOLDER}/**/*.css`;

module.exports = {
  DEST_FOLDER,
  SRC_FOLDER,
  TS_SRC_FILES,
  CSS_SRC_FILES
};